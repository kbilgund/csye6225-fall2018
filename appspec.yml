version: 0.0
os: linux

files:             
  - source: ./target/cloudClass1-1.0-SNAPSHOT.jar
    destination: /home/centos/

hooks:
  ApplicationStart:
   - location: ./applicationStart.sh
     timeout: 60
     runas: centos
